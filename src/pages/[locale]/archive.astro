---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Archive from "../../islands/Archive.tsx";
import { ALL_LANGS } from "../../lib/i18n";
import type { Lang } from "../../lib/i18n";

export function getStaticPaths() {
  return ALL_LANGS.filter((l) => l !== "en").map((locale) => ({
    params: { locale },
  }));
}

const { locale } = Astro.params as { locale: Lang };
---

<BaseLayout title="Archive" description="" locale={locale} wide>
  <Archive client:only="react" />
</BaseLayout>
