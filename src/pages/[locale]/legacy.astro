---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Legacy from "../../islands/Legacy.tsx";
import { ALL_LANGS } from "../../lib/i18n";
import type { Lang } from "../../lib/i18n";

export function getStaticPaths() {
  return ALL_LANGS.filter((l) => l !== "en").map((locale) => ({
    params: { locale },
  }));
}

const { locale } = Astro.params as { locale: Lang };
---

<BaseLayout title="Portfolio" description="" locale={locale} wide>
  <Legacy client:only="react" />
</BaseLayout>
